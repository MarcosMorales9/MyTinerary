import {
    Text, 
    View,
    StyleSheet,
    Image,
    Dimensions,
    Dimensions,
    SafeAreaView,
    Animated,
    FlatList
    } from 'react-native'
import React, { Component } from 'react'

export default function carrousel() {
    return (
      <View>
      </View>
    )
  }
